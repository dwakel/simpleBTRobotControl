<Page
    x:Class="RobotZ.PairDevice"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RobotZ"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        
        <!--<ListView Name="deviceListView"
                  ItemsSource="{Binding}"
                  IsItemClickEnabled="True"
                  ItemClick="deviceListView_ItemClick"
                  HorizontalAlignment="Stretch">
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <ItemsWrapGrid/>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
            
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Vertical">
                        <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#59642;" Margin="10" Grid.RowSpan="3"/>
                        <TextBlock Grid.Column="1" Grid.Row="0" Text="{Binding Id}"/>
                        <TextBlock Grid.Column="1" Grid.Row="1" Text="{Binding Name}"/>
                        <StackPanel Grid.Column="1" Grid.Row="2" Orientation="Horizontal">
                            <TextBlock Text="Can be paired: "/>
                            <TextBlock Text="{Binding Pairing, Converter={StaticResource pairingConv}, Mode=OneWay}"/>
                        </StackPanel>
                      </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
            
        </ListView>-->
    </Grid>
</Page>

